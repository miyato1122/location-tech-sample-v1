# Implementation Plan

- [ ] 1. 基盤と前提条件の整備
- [x] 1.1 背景地図カタログと選択状態契約を定義する
  - 3種類の背景地図を安定したID・表示名・表示元情報・出典対応で定義する
  - 切り替え処理と表示処理が同じカタログを参照できるようにする
  - 完了条件: アプリ起動時に背景地図候補が3件参照できる
  - _Requirements: 1.2,3.1,4.1,4.2_
- [x] 1.2 左下セレクターと出典/失敗表示のUI受け皿契約を整える
  - 左下セレクターの配置先と出典/失敗メッセージの表示先を定義する
  - 表示名・状態クラス・表示切替に使う命名を統一する
  - 完了条件: セレクターと出典/失敗表示をマウントできる受け皿が初期表示で存在する
  - _Requirements: 1.1,3.1,4.1,4.3_
- [x] 1.3 背景地図失敗イベントと失敗状態チャネルを準備する
  - 読み込み/描画失敗を検知できるフックを定義する
  - 制御ロジックとUIが共通で読める失敗状態の受け渡し契約を定義する
  - 完了条件: 失敗イベント発生時に専用の失敗状態チャネルが更新される
  - _Requirements: 1.5,4.3_
- [x] 1.4 背景地図ロジック向け自動テスト実行基盤を整備する
  - 切り替えロジックと表示同期の検証を実行できるテスト環境を準備する
  - ローカル実行手順を固定し、継続的に再実行できる状態にする
  - 完了条件: ローカルで自動テストコマンドが正常終了する
  - _Requirements: 2.4,4.3_

- [ ] 2. 背景地図切り替えコアロジックの実装
- [x] 2.1 背景地図データ定義と排他初期表示を実装する
  - 背景地図3種の表示定義を追加し、初期表示時の排他ルールを適用する
  - 起動直後に背景地図が1種類のみ表示される初期状態を保証する
  - 完了条件: 初期描画時に有効な背景地図が常に1種類である
  - _Depends: 1.1_
  - _Requirements: 1.2,1.3,1.4_
- [x] 2.2 背景地図切り替えと無効選択フォールバックを実装する
  - 有効選択時に選択背景へ切り替える処理を実装する
  - 無効選択時は直前の有効状態を維持し、状態不変として扱う
  - 完了条件: 無効選択時に直前背景が保持され、状態変更なしが判定できる
  - _Depends: 2.1_
  - _Requirements: 1.3,1.4,1.5_
- [x] 2.3 背景地図失敗時の安定状態維持ロジックを実装する
  - 失敗検知時に直前の安定背景を維持する制御を実装する
  - 失敗状態をUI側で判別可能な形で保持する
  - 完了条件: 背景読み込み失敗時でも直前の安定背景が維持される
  - _Depends: 1.3,2.2_
  - _Requirements: 1.5,4.3_

- [ ] 3. 既存機能との継続性統合
- [ ] 3.1 背景切り替え時のハザード表示継続を統合する
  - 背景切り替え前後でハザード表示状態が維持されるよう統合する
  - 既存ハザード操作との干渉がないことを確認する
  - 完了条件: 背景切り替え後も有効ハザード表示が維持される
  - _Depends: 2.2_
  - _Requirements: 2.1_
- [ ] 3.2 背景切り替え時の避難施設表示継続を統合する
  - 背景切り替え前後で避難施設表示状態が維持されるよう統合する
  - 既存避難施設操作との干渉がないことを確認する
  - 完了条件: 背景切り替え後も有効避難施設表示が維持される
  - _Depends: 2.2_
  - _Requirements: 2.2_
- [ ] 3.3 現在地ルート継続と再読み込み不要動作を統合する
  - 背景切り替え中でも現在地連動表示とルート更新を継続させる
  - 画面再読み込みなしで地図操作を継続できる状態を保証する
  - 完了条件: 背景切り替え後も現在地ルート表示と操作継続が確認できる
  - _Depends: 2.2_
  - _Requirements: 2.3,2.4_

- [ ] 4. UI・出典・メッセージ体験の実装
- [ ] 4.1 左下セレクターの操作と選択中表示を実装する
  - 背景地図の選択操作を左下UIで行えるようにする
  - 選択中項目を視覚的に区別して表示する
  - 完了条件: 3種類の背景地図を左下UIで選択でき、選択中が判別できる
  - _Requirements: 1.1,3.1,3.2_
- [ ] 4.2 狭画面でも地図閲覧を妨げないレスポンシブ表示を実装する
  - 主要な地図閲覧領域を維持したままセレクターを表示する
  - モバイル幅でも操作可能性と視認性を維持する
  - 完了条件: 狭画面でセレクターが利用可能かつ地図の主要表示が確保される
  - _Depends: 4.1_
  - _Requirements: 3.3_
- [ ] 4.3 背景地図選択に連動した出典表示同期を実装する
  - 選択中背景地図に対応した出典を表示する
  - 背景切り替え時に出典表示が即時更新されるようにする
  - 完了条件: 背景切り替えのたびに出典表示内容が一致して更新される
  - _Depends: 2.2,4.1_
  - _Requirements: 4.1,4.2_
- [ ] 4.4 失敗/回復メッセージ表示を一貫化する
  - 失敗状態と回復状態を利用者が判別できる表示にする
  - 連続切り替え時でもメッセージ遷移が破綻しないよう統一する
  - 完了条件: 失敗発生と回復の両方を利用者が画面上で判別できる
  - _Depends: 2.3,4.1_
  - _Requirements: 4.3,1.5_

- [ ] 5. テストと受け入れ検証
- [ ] 5.1 切り替えロジックと出典同期の自動テストを実装する
  - カタログ整合、排他表示、無効選択保持、出典同期を自動検証する
  - 失敗時に回帰を検知できるテストケースを追加する
  - 完了条件: 切り替えロジックと出典同期の自動テストが成功する
  - _Depends: 1.4,2.2,4.3_
  - _Requirements: 1.2,1.4,1.5,4.2_
- [ ] 5.2 継続性統合の自動検証を実装する
  - 背景切り替え前後でハザード/避難施設/ルート継続を検証する
  - 既存機能との非回帰を確認する統合検証を追加する
  - 完了条件: 継続性に関する統合検証が成功する
  - _Depends: 3.1,3.2,3.3_
  - _Requirements: 2.1,2.2,2.3,2.4_
- [ ] 5.3 レスポンシブ表示と失敗表示の受け入れ検証を実装する
  - 狭画面での視認性・操作性を検証する
  - 失敗表示と回復表示の判別可能性を検証する
  - 完了条件: 狭画面表示と失敗/回復表示の受け入れ検証が成功する
  - _Depends: 4.2,4.4_
  - _Requirements: 3.3,4.3,1.1_
