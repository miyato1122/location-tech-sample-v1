# TASK-0003: ホバー時カーソル変更

## タスク概要

- **タスクタイプ**: DIRECT
- **推定工数**: 1時間
- **依存タスク**: TASK-0001（マップ初期化）、shelter-display/TASK-0001（避難場所レイヤー定義）

## 実装ファイル

- `main.js:519-540`

## 実装詳細

### mousemove イベント処理

```javascript
map.on('mousemove', (e) => {
    const features = map.queryRenderedFeatures(e.point, {
        layers: ['skhb-1-layer', ..., 'skhb-8-layer'],
    });
    if (features.length > 0) {
        map.getCanvas().style.cursor = 'pointer';
    } else {
        map.getCanvas().style.cursor = '';
    }
});
```

### 動作仕様

- 避難場所アイコン上にカーソルが重なると `cursor: pointer`（手のひらアイコン）
- それ以外では `cursor: ''`（デフォルトカーソルに戻す）

## テスト実装状況

- [ ] 単体テスト（未実装）
- [ ] 統合テスト（未実装）
- [ ] E2Eテスト（未実装）
