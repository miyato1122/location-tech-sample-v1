# TASK-0002: Service Worker 設定（最小実装）

## タスク概要

- **タスクタイプ**: DIRECT
- **推定工数**: 0.5時間
- **依存タスク**: TASK-0001（PWAマニフェスト設定）

## 実装ファイル

- `public/sw.js`

## 実装詳細

### 現在の実装（最小実装）

```javascript
// sw.js
self.addEventListener('fetch', (event) => {
    // 何もしない（パススルー）
});
```

Service Worker は登録されているが、実質的なオフラインキャッシュ機能はない。PWA として認識されるための最低限の実装のみ。

### 推奨される改善（未実装）

オフライン利用を実現するには以下の実装が必要:

```javascript
// キャッシュ名
const CACHE_NAME = 'bousai-map-v1';

// インストール時にキャッシュ
self.addEventListener('install', (event) => {
    event.waitUntil(
        caches.open(CACHE_NAME).then((cache) =>
            cache.addAll(['./', './index.html', './assets/index.js'])
        )
    );
});

// フェッチ時にキャッシュから返却（Cache First戦略）
self.addEventListener('fetch', (event) => {
    event.respondWith(
        caches.match(event.request).then((cached) =>
            cached || fetch(event.request)
        )
    );
});
```

## テスト実装状況

- [ ] 単体テスト（未実装）
- [ ] オフライン動作確認（未実施）
