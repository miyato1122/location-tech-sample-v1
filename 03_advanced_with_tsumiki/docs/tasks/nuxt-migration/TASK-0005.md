# TASK-0005: テスト基盤整備（Vitest + Playwright）

**タスクID**: TASK-0005
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - プロジェクト基盤構築
**ステータス**: ✅ 完了
**完了日**: 2026-04-20
**信頼性レベル**: 🔵 *REQ-090 + ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/nuxt-migration/requirements.md) - REQ-090
- **設計文書**: [📐 architecture.md](../../design/nuxt-migration/architecture.md)

## タスク概要

VitestとPlaywrightのテスト環境をセットアップする。
`@nuxt/test-utils` を用いた単体テストと、Playwright E2Eテストの実行環境を整備する。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md)
- **後続タスク**: [TASK-0015](TASK-0015.md), [TASK-0016](TASK-0016.md)

## 完了条件

- [x] `vitest.config.ts` が作成されている（happy-dom環境、~エイリアス設定済み）
- [x] `playwright.config.ts` が作成されている
- [x] `tests/unit/` と `tests/e2e/` ディレクトリが作成されている
- [x] `npm run test:unit` でVitestが実行できる
- [x] `npm run test:e2e` でPlaywrightが実行できる（設定済み）
- [x] サンプルテスト4件がパス（tests/unit/types.test.ts）

---

## 実装詳細

### 1. Vitest設定 🔵

**信頼性**: 🔵 *REQ-090 + @nuxt/test-utils ドキュメントより*

**実装ファイル**: `vitest.config.ts`

```typescript
import { defineVitestConfig } from '@nuxt/test-utils/config'

export default defineVitestConfig({
  test: {
    environment: 'nuxt',
    globals: true,
  },
})
```

**package.json スクリプト追加**:
```json
{
  "scripts": {
    "test:unit": "vitest run",
    "test:unit:watch": "vitest"
  }
}
```

### 2. Playwright設定 🔵

**信頼性**: 🔵 *REQ-090 + Playwright公式ドキュメントより*

**実装ファイル**: `playwright.config.ts`

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  retries: process.env.CI ? 2 : 0,
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'Mobile Chrome', use: { ...devices['Pixel 5'] } },
  ],
  webServer: {
    command: 'npm run preview',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
```

**package.json スクリプト追加**:
```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
```

### 3. テストディレクトリ構造作成 🔵

**信頼性**: 🔵 *アーキテクチャ設計より*

```
tests/
├── unit/
│   └── .gitkeep
└── e2e/
    └── .gitkeep
```

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- Playwright E2EテストはNuxtビルド後のプレビューサーバーで実行するため `webServer` に `npm run preview` を指定
- `@playwright/test` のインストール後に `npx playwright install` でブラウザをインストール

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3件 | 0件 | 0件 | 3件 |

- 🔵 **青信号**: 3件 (100%)
- 🟡 **黄信号**: 0件 (0%)
- 🔴 **赤信号**: 0件 (0%)

**品質評価**: ✅ 高品質
