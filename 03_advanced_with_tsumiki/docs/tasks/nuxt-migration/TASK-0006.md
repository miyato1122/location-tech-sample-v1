# TASK-0006: useMap composable実装

**タスクID**: TASK-0006
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 2 - Composables実装
**信頼性レベル**: 🔵 *REQ-010, EDGE-202より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/nuxt-migration/requirements.md) - REQ-010
- **設計文書**: [📐 architecture.md](../../design/nuxt-migration/architecture.md)
- **型定義**: [🔷 interfaces.ts](../../design/nuxt-migration/interfaces.ts)

## タスク概要

MapLibre GL JSのマップ初期化ロジックを `composables/useMap.ts` として実装する。
`onMounted` でマップ初期化、`onUnmounted` で `map.remove()` を呼びメモリリークを防ぐ。
`Ref<maplibregl.Map | null>` を返す。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0007](TASK-0007.md), [TASK-0008](TASK-0008.md), [TASK-0009](TASK-0009.md), [TASK-0010](TASK-0010.md), [TASK-0011](TASK-0011.md), [TASK-0013](TASK-0013.md)

## 完了条件

- [x] `composables/useMap.ts` が作成されている
- [x] `onMounted` でマップが初期化される（center: [138, 37], zoom: 5, minZoom: 5, maxBounds: [122, 20, 154, 50]）
- [x] `onUnmounted` で `map.remove()` が呼ばれる
- [x] `Ref<maplibregl.Map | null>` を返す
- [x] TypeScript strict モードでエラーなし
- [x] `useMap` の単体テストがパスする

## 実装詳細

### 1. composables/useMap.ts 実装 🔵（REQ-010, EDGE-201, EDGE-202より）

```typescript
import { ref, onMounted, onUnmounted } from 'vue'
import type { Map as MaplibreMap } from 'maplibre-gl'
import { MAP_INIT_CONFIG } from '~/types'
import type { UseMapReturn } from '~/types'

export function useMap(containerId: string): UseMapReturn {
  const map = ref<MaplibreMap | null>(null)

  onMounted(async () => {
    // ブラウザ環境でのみimport（SSR無効でも念のため）
    const maplibregl = await import('maplibre-gl')
    map.value = new maplibregl.Map({
      container: containerId,
      style: {
        version: 8,
        sources: {
          osm: {
            type: 'raster',
            tiles: ['https://tile.openstreetmap.org/{z}/{x}/{y}.png'],
            tileSize: 256,
            attribution: '© OpenStreetMap contributors',
          },
        },
        layers: [{ id: 'osm', type: 'raster', source: 'osm' }],
      },
      zoom: MAP_INIT_CONFIG.zoom,
      center: MAP_INIT_CONFIG.center,
      minZoom: MAP_INIT_CONFIG.minZoom,
      maxZoom: MAP_INIT_CONFIG.maxZoom,
      maxBounds: MAP_INIT_CONFIG.maxBounds,
    })
  })

  onUnmounted(() => {
    map.value?.remove()
    map.value = null
  })

  return { map }
}
```

### 2. 単体テスト（tests/unit/useMap.test.ts）🟡（テスト環境でMapLibreモックが必要）

```typescript
// MapLibreはブラウザ専用のためモックが必要
// map.remove()がonUnmounted時に呼ばれることを確認
```

## 注意事項

- MapLibre GL JSはブラウザ専用のため、動的import（`await import('maplibre-gl')`）を使用
- `ssr: false` 設定があるためSSR実行はないが、明示的な動的importで意図を明確にする

## 信頼性サマリー

| 信頼性 | 件数 | 割合 |
|--------|------|------|
| 🔵 確実 | 3件 | 75% |
| 🟡 推測 | 1件 | 25% |
| 🔴 根拠なし | 0件 | 0% |
