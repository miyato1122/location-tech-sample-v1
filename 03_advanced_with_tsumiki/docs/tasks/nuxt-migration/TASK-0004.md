# TASK-0004: @vite-pwa/nuxt PWA設定

**タスクID**: TASK-0004
**タスクタイプ**: DIRECT
**推定工数**: 3時間
**フェーズ**: Phase 1 - プロジェクト基盤構築
**ステータス**: ✅ 完了
**完了日**: 2026-04-20
**信頼性レベル**: 🔵 *REQ-080 + ユーザーヒアリングより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/nuxt-migration/requirements.md) - REQ-080, NFR-301
- **設計文書**: [📐 architecture.md](../../design/nuxt-migration/architecture.md)

## タスク概要

`@vite-pwa/nuxt` を使用してPWA機能を設定する。
アプリ名・アイコン・display設定・Service Workerのキャッシュ戦略を構成する。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0003](TASK-0003.md)
- **後続タスク**: [TASK-0017](TASK-0017.md)

## 完了条件

- [x] `nuxt.config.ts` に `@vite-pwa/nuxt` のPWA設定が追加されている
- [x] ビルド後に `manifest.webmanifest` が生成される（ビルド時確認）
- [x] Service Workerが登録される（devOptions.enabled: false で開発中は無効）
- [x] display: standalone でホーム画面インストールが可能
- [x] アイコン（192, 256, 384, 512px）が設定されている

---

## 実装詳細

### 1. nuxt.config.ts PWA設定 🔵

**信頼性**: 🔵 *REQ-080 + ユーザーヒアリング + 既存 public/manifest.json より*

**実装ファイル**: `nuxt.config.ts`

```typescript
export default defineNuxtConfig({
  ssr: false,
  modules: ['@vite-pwa/nuxt'],
  pwa: {
    manifest: {
      name: '防災マップ',
      short_name: '防災マップ',
      display: 'standalone',
      background_color: '#ffffff',
      theme_color: '#3388ff',
      orientation: 'any',
      start_url: '/',
      scope: '/',
      icons: [
        { src: 'icon192.png', sizes: '192x192', type: 'image/png' },
        { src: 'icon256.png', sizes: '256x256', type: 'image/png' },
        { src: 'icon384.png', sizes: '384x384', type: 'image/png' },
        { src: 'icon512.png', sizes: '512x512', type: 'image/png' },
      ],
    },
    workbox: {
      navigateFallback: '/',
      globPatterns: ['**/*.{js,css,html,png,ico}'],
      // PBFタイルは大容量のため除外（ビルド時にpublicに含まれるが個別キャッシュ不要）
      globIgnores: ['skhb/**'],
    },
    devOptions: {
      enabled: false,  // 開発中はSW無効（デバッグしやすくするため）
    },
  },
  typescript: { strict: true },
})
```

### 2. PWA動作確認手順 🟡

**信頼性**: 🟡 *@vite-pwa/nuxt のドキュメントから妥当な推測*

```bash
npm run build
npm run preview
# ブラウザのDevToolsでService Worker登録を確認
```

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- `devOptions.enabled: false` にすることで開発中のSWキャッシュ問題を回避
- PBFタイルはglobIgnoresで除外する（容量大のため）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1件 | 1件 | 0件 | 2件 |

- 🔵 **青信号**: 1件 (50%)
- 🟡 **黄信号**: 1件 (50%)
- 🔴 **赤信号**: 0件 (0%)

**品質評価**: ✅ 高品質
